openapi: "3.1.0"
info:
  title: beaver
  version: "0.19.0"
paths:
  /events:
    get:
      tags:
        - Events
      summary: List events
      description: List events that match the request.
      operationId: EventsList
      parameters:
        - name: limit
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/events_models_ListRequestLimit"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/events_models_ListRequestLimit"
                title: ListRequestLimit
              - type: "null"
            description: Maximum number of events to return. Default is 10.
          description: Maximum number of events to return. Default is 10.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
        - name: offset
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/events_models_ListRequestOffset"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/events_models_ListRequestOffset"
                title: ListRequestOffset
              - type: "null"
            description: Number of events to skip.
          description: Number of events to skip.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
        - name: where
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/events_models_ListRequestWhere"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/events_models_ListRequestWhere"
                title: ListRequestWhere
              - type: "null"
            description: Filter to apply to find events.
          description: Filter to apply to find events.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
        - name: query
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/ListRequestQuery"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/ListRequestQuery"
                title: ListRequestQuery
              - type: "null"
            description: Advanced query to apply to find events.
          description: Advanced query to apply to find events.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
        - name: include
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/events_models_ListRequestInclude"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/events_models_ListRequestInclude"
                title: ListRequestInclude
              - type: "null"
            description: Relations to include in the response.
          description: Relations to include in the response.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
        - name: order
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/events_models_ListRequestOrder"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/events_models_ListRequestOrder"
                title: ListRequestOrder
              - type: "null"
            description: Order to apply to the results.
          description: Order to apply to the results.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
      responses:
        "200":
          description: Request fulfilled, document follows
          headers: {}
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/events_models_ListResponseResults"
        "400":
          description: Bad request syntax or unsupported method
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Validation Exception
                examples:
                  - status_code: 400
                    detail: Bad Request
                    extra: {}
      deprecated: false
    post:
      tags:
        - Events
      summary: Create event
      description: Create a new event.
      operationId: EventsCreate
      parameters:
        - name: include
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/events_models_CreateRequestInclude"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/events_models_CreateRequestInclude"
                title: CreateRequestInclude
              - type: "null"
            description: Relations to include in the response.
          description: Relations to include in the response.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/events_models_CreateRequestData"
        required: true
      responses:
        "201":
          description: Document created, URL follows
          headers: {}
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateResponseEvent"
        "400":
          description: Bad request syntax or unsupported method
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Validation Exception
                examples:
                  - status_code: 400
                    detail: Bad Request
                    extra: {}
        "409":
          description: Conflict
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Conflict Exception
                examples:
                  - status_code: 409
                    detail: Conflict
                    extra: {}
      deprecated: false
  /events/{id}:
    get:
      tags:
        - Events
      summary: Get event
      description: Get an event by ID.
      operationId: EventsIdGet
      parameters:
        - name: id
          in: path
          schema:
            $ref: "#/components/schemas/events_models_GetRequestId"
          required: true
          deprecated: false
        - name: include
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/events_models_GetRequestInclude"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/events_models_GetRequestInclude"
                title: GetRequestInclude
              - type: "null"
            description: Relations to include in the response.
          description: Relations to include in the response.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
      responses:
        "200":
          description: Request fulfilled, document follows
          headers: {}
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetResponseEvent"
        "400":
          description: Bad request syntax or unsupported method
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Validation Exception
                examples:
                  - status_code: 400
                    detail: Bad Request
                    extra: {}
        "404":
          description: Nothing matches the given URI
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Not Found Exception
                examples:
                  - status_code: 404
                    detail: Not Found
                    extra: {}
      deprecated: false
    delete:
      tags:
        - Events
      summary: Delete event
      description: Delete an event by ID.
      operationId: EventsIdDelete
      parameters:
        - name: id
          in: path
          schema:
            $ref: "#/components/schemas/events_models_DeleteRequestId"
          required: true
          deprecated: false
      responses:
        "204":
          description: Request fulfilled, nothing follows
          headers: {}
        "400":
          description: Bad request syntax or unsupported method
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Validation Exception
                examples:
                  - status_code: 400
                    detail: Bad Request
                    extra: {}
        "404":
          description: Nothing matches the given URI
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Not Found Exception
                examples:
                  - status_code: 404
                    detail: Not Found
                    extra: {}
      deprecated: false
    patch:
      tags:
        - Events
      summary: Update event
      description: Update an event by ID.
      operationId: EventsIdUpdate
      parameters:
        - name: id
          in: path
          schema:
            $ref: "#/components/schemas/events_models_UpdateRequestId"
          required: true
          deprecated: false
        - name: include
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/events_models_UpdateRequestInclude"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/events_models_UpdateRequestInclude"
                title: UpdateRequestInclude
              - type: "null"
            description: Relations to include in the response.
          description: Relations to include in the response.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/events_models_UpdateRequestData"
        required: true
      responses:
        "200":
          description: Request fulfilled, document follows
          headers: {}
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateResponseEvent"
        "400":
          description: Bad request syntax or unsupported method
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Validation Exception
                examples:
                  - status_code: 400
                    detail: Bad Request
                    extra: {}
        "404":
          description: Nothing matches the given URI
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Not Found Exception
                examples:
                  - status_code: 404
                    detail: Not Found
                    extra: {}
        "409":
          description: Conflict
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Conflict Exception
                examples:
                  - status_code: 409
                    detail: Conflict
                    extra: {}
      deprecated: false
  /events/{id}/split:
    post:
      tags:
        - Events
      summary: Split event
      description: Split an event by ID.
      operationId: EventsIdSplitSplit
      parameters:
        - name: id
          in: path
          schema:
            $ref: "#/components/schemas/SplitRequestId"
          required: true
          deprecated: false
        - name: include
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/SplitRequestInclude"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/SplitRequestInclude"
                title: SplitRequestInclude
              - type: "null"
            description: Relations to include in the response.
          description: Relations to include in the response.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SplitRequestData"
        required: true
      responses:
        "201":
          description: Document created, URL follows
          headers: {}
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SplitResponseResult"
        "400":
          description: Bad request syntax or unsupported method
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Validation Exception
                examples:
                  - status_code: 400
                    detail: Bad Request
                    extra: {}
        "404":
          description: Nothing matches the given URI
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Not Found Exception
                examples:
                  - status_code: 404
                    detail: Not Found
                    extra: {}
        "409":
          description: Conflict
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Conflict Exception
                examples:
                  - status_code: 409
                    detail: Conflict
                    extra: {}
      deprecated: false
  /instances:
    get:
      tags:
        - Instances
      summary: List instances
      description: List instances.
      operationId: InstancesList
      parameters:
        - name: start
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/ListRequestStart"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/ListRequestStart"
                title: ListRequestStart
              - type: "null"
            description: >-
              Start datetime in UTC to filter instances. Default is now.
          description: >-
            Start datetime in UTC to filter instances. Default is now.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
        - name: end
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/ListRequestEnd"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/ListRequestEnd"
                title: ListRequestEnd
              - type: "null"
            description: >-
              End datetime in UTC to filter instances. Default is now.
          description: End datetime in UTC to filter instances. Default is now.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
        - name: where
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/instances_models_ListRequestWhere"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/instances_models_ListRequestWhere"
                title: ListRequestWhere
              - type: "null"
            description: Filter to apply to find instances.
          description: Filter to apply to find instances.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
        - name: include
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/instances_models_ListRequestInclude"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/instances_models_ListRequestInclude"
                title: ListRequestInclude
              - type: "null"
            description: Relations to include in the response.
          description: Relations to include in the response.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
        - name: order
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/instances_models_ListRequestOrder"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/instances_models_ListRequestOrder"
                title: ListRequestOrder
              - type: "null"
            description: Order to apply to the results.
          description: Order to apply to the results.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
      responses:
        "200":
          description: Request fulfilled, document follows
          headers: {}
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/instances_models_ListResponseResults"
        "400":
          description: Bad request syntax or unsupported method
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Validation Exception
                examples:
                  - status_code: 400
                    detail: Bad Request
                    extra: {}
      deprecated: false
    post:
      tags:
        - Instances
      summary: Create instance
      description: Create a new instance.
      operationId: InstancesCreate
      parameters:
        - name: include
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/instances_models_CreateRequestInclude"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/instances_models_CreateRequestInclude"
                title: CreateRequestInclude
              - type: "null"
            description: Relations to include in the response.
          description: Relations to include in the response.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/instances_models_CreateRequestData"
        required: true
      responses:
        "201":
          description: Document created, URL follows
          headers: {}
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateResponseInstance"
        "400":
          description: Bad request syntax or unsupported method
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Validation Exception
                examples:
                  - status_code: 400
                    detail: Bad Request
                    extra: {}
        "409":
          description: Conflict
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Conflict Exception
                examples:
                  - status_code: 409
                    detail: Conflict
                    extra: {}
      deprecated: false
  /instances/{eventId}/{start}:
    get:
      tags:
        - Instances
      summary: Get instance
      description: Get an instance by event ID and start datetime.
      operationId: InstancesEventidStartGet
      parameters:
        - name: eventId
          in: path
          schema:
            $ref: "#/components/schemas/GetRequestEventId"
          required: true
          deprecated: false
        - name: start
          in: path
          schema:
            $ref: "#/components/schemas/GetRequestStart"
          description: Datetime without timezone.
          required: true
          deprecated: false
        - name: include
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/instances_models_GetRequestInclude"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/instances_models_GetRequestInclude"
                title: GetRequestInclude
              - type: "null"
            description: Relations to include in the response.
          description: Relations to include in the response.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
      responses:
        "200":
          description: Request fulfilled, document follows
          headers: {}
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetResponseInstance"
        "400":
          description: Bad request syntax or unsupported method
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Validation Exception
                examples:
                  - status_code: 400
                    detail: Bad Request
                    extra: {}
        "404":
          description: Nothing matches the given URI
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Not Found Exception
                examples:
                  - status_code: 404
                    detail: Not Found
                    extra: {}
      deprecated: false
    delete:
      tags:
        - Instances
      summary: Delete instance
      description: Delete an instance by event ID and start datetime.
      operationId: InstancesEventidStartDelete
      parameters:
        - name: eventId
          in: path
          schema:
            $ref: "#/components/schemas/DeleteRequestEventId"
          required: true
          deprecated: false
        - name: start
          in: path
          schema:
            $ref: "#/components/schemas/DeleteRequestStart"
          description: Datetime without timezone.
          required: true
          deprecated: false
      responses:
        "204":
          description: Request fulfilled, nothing follows
          headers: {}
        "400":
          description: Bad request syntax or unsupported method
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Validation Exception
                examples:
                  - status_code: 400
                    detail: Bad Request
                    extra: {}
        "404":
          description: Nothing matches the given URI
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Not Found Exception
                examples:
                  - status_code: 404
                    detail: Not Found
                    extra: {}
      deprecated: false
    patch:
      tags:
        - Instances
      summary: Update instance
      description: Update an instance by event ID and start datetime.
      operationId: InstancesEventidStartUpdate
      parameters:
        - name: eventId
          in: path
          schema:
            $ref: "#/components/schemas/UpdateRequestEventId"
          required: true
          deprecated: false
        - name: start
          in: path
          schema:
            $ref: "#/components/schemas/UpdateRequestStart"
          description: Datetime without timezone.
          required: true
          deprecated: false
        - name: include
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/instances_models_UpdateRequestInclude"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/instances_models_UpdateRequestInclude"
                title: UpdateRequestInclude
              - type: "null"
            description: Relations to include in the response.
          description: Relations to include in the response.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/instances_models_UpdateRequestData"
        required: true
      responses:
        "200":
          description: Request fulfilled, document follows
          headers: {}
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateResponseInstance"
        "400":
          description: Bad request syntax or unsupported method
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Validation Exception
                examples:
                  - status_code: 400
                    detail: Bad Request
                    extra: {}
        "404":
          description: Nothing matches the given URI
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Not Found Exception
                examples:
                  - status_code: 404
                    detail: Not Found
                    extra: {}
        "409":
          description: Conflict
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Conflict Exception
                examples:
                  - status_code: 409
                    detail: Conflict
                    extra: {}
      deprecated: false
  /ping:
    get:
      tags:
        - Ping
      summary: Ping
      description: Ping.
      operationId: PingPing
      responses:
        "204":
          description: Request fulfilled, nothing follows
          headers:
            Cache-Control:
              schema:
                type: string
              required: true
              deprecated: false
      deprecated: false
    head:
      tags:
        - Ping
      summary: Ping headers
      description: Ping headers.
      operationId: PingHeadping
      responses:
        "204":
          description: Request fulfilled, nothing follows
          headers:
            Cache-Control:
              schema:
                type: string
              required: true
              deprecated: false
      deprecated: false
  /shows:
    get:
      tags:
        - Shows
      summary: List shows
      description: List shows that match the request.
      operationId: ShowsList
      parameters:
        - name: limit
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/shows_models_ListRequestLimit"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/shows_models_ListRequestLimit"
                title: ListRequestLimit
              - type: "null"
            description: Maximum number of shows to return. Default is 10.
          description: Maximum number of shows to return. Default is 10.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
        - name: offset
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/shows_models_ListRequestOffset"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/shows_models_ListRequestOffset"
                title: ListRequestOffset
              - type: "null"
            description: Number of shows to skip.
          description: Number of shows to skip.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
        - name: where
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/shows_models_ListRequestWhere"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/shows_models_ListRequestWhere"
                title: ListRequestWhere
              - type: "null"
            description: Filter to apply to find shows.
          description: Filter to apply to find shows.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
        - name: include
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/shows_models_ListRequestInclude"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/shows_models_ListRequestInclude"
                title: ListRequestInclude
              - type: "null"
            description: Relations to include in the response.
          description: Relations to include in the response.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
        - name: order
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/shows_models_ListRequestOrder"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/shows_models_ListRequestOrder"
                title: ListRequestOrder
              - type: "null"
            description: Order to apply to the results.
          description: Order to apply to the results.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
      responses:
        "200":
          description: Request fulfilled, document follows
          headers: {}
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/shows_models_ListResponseResults"
        "400":
          description: Bad request syntax or unsupported method
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Validation Exception
                examples:
                  - status_code: 400
                    detail: Bad Request
                    extra: {}
      deprecated: false
    post:
      tags:
        - Shows
      summary: Create show
      description: Create a new show.
      operationId: ShowsCreate
      parameters:
        - name: include
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/shows_models_CreateRequestInclude"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/shows_models_CreateRequestInclude"
                title: CreateRequestInclude
              - type: "null"
            description: Relations to include in the response.
          description: Relations to include in the response.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/shows_models_CreateRequestData"
        required: true
      responses:
        "201":
          description: Document created, URL follows
          headers: {}
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateResponseShow"
        "400":
          description: Bad request syntax or unsupported method
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Validation Exception
                examples:
                  - status_code: 400
                    detail: Bad Request
                    extra: {}
        "409":
          description: Conflict
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Conflict Exception
                examples:
                  - status_code: 409
                    detail: Conflict
                    extra: {}
      deprecated: false
  /shows/{id}:
    get:
      tags:
        - Shows
      summary: Get show
      description: Get a show by ID.
      operationId: ShowsIdGet
      parameters:
        - name: id
          in: path
          schema:
            $ref: "#/components/schemas/shows_models_GetRequestId"
          required: true
          deprecated: false
        - name: include
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/shows_models_GetRequestInclude"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/shows_models_GetRequestInclude"
                title: GetRequestInclude
              - type: "null"
            description: Relations to include in the response.
          description: Relations to include in the response.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
      responses:
        "200":
          description: Request fulfilled, document follows
          headers: {}
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetResponseShow"
        "400":
          description: Bad request syntax or unsupported method
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Validation Exception
                examples:
                  - status_code: 400
                    detail: Bad Request
                    extra: {}
        "404":
          description: Nothing matches the given URI
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Not Found Exception
                examples:
                  - status_code: 404
                    detail: Not Found
                    extra: {}
      deprecated: false
    delete:
      tags:
        - Shows
      summary: Delete show
      description: Delete a show by ID.
      operationId: ShowsIdDelete
      parameters:
        - name: id
          in: path
          schema:
            $ref: "#/components/schemas/shows_models_DeleteRequestId"
          required: true
          deprecated: false
      responses:
        "204":
          description: Request fulfilled, nothing follows
          headers: {}
        "400":
          description: Bad request syntax or unsupported method
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Validation Exception
                examples:
                  - status_code: 400
                    detail: Bad Request
                    extra: {}
        "404":
          description: Nothing matches the given URI
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Not Found Exception
                examples:
                  - status_code: 404
                    detail: Not Found
                    extra: {}
      deprecated: false
    patch:
      tags:
        - Shows
      summary: Update show
      description: Update a show by ID.
      operationId: ShowsIdUpdate
      parameters:
        - name: id
          in: path
          schema:
            $ref: "#/components/schemas/shows_models_UpdateRequestId"
          required: true
          deprecated: false
        - name: include
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/shows_models_UpdateRequestInclude"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/shows_models_UpdateRequestInclude"
                title: UpdateRequestInclude
              - type: "null"
            description: Relations to include in the response.
          description: Relations to include in the response.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/shows_models_UpdateRequestData"
        required: true
      responses:
        "200":
          description: Request fulfilled, document follows
          headers: {}
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateResponseShow"
        "400":
          description: Bad request syntax or unsupported method
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Validation Exception
                examples:
                  - status_code: 400
                    detail: Bad Request
                    extra: {}
        "404":
          description: Nothing matches the given URI
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Not Found Exception
                examples:
                  - status_code: 404
                    detail: Not Found
                    extra: {}
        "409":
          description: Conflict
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Conflict Exception
                examples:
                  - status_code: 409
                    detail: Conflict
                    extra: {}
      deprecated: false
  /sse:
    get:
      tags:
        - SSE
      summary: Get SSE stream
      description: Get a stream of Server-Sent Events.
      operationId: SseSubscribe
      parameters:
        - name: types
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/SubscribeRequestTypes"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/SubscribeRequestTypes"
                title: SubscribeRequestTypes
              - type: "null"
            description: Types of events to subscribe to.
          description: Types of events to subscribe to.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
      responses:
        "200":
          description: Request fulfilled, stream of Server-Sent Events follows
          headers:
            Cache-Control:
              schema:
                type: string
              required: true
              deprecated: false
            Connection:
              schema:
                type: string
              required: true
              deprecated: false
          content:
            text/event-stream:
              schema:
                type: string
        "400":
          description: Bad request syntax or unsupported method
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Validation Exception
                examples:
                  - status_code: 400
                    detail: Bad Request
                    extra: {}
      deprecated: false
  /test:
    get:
      tags:
        - Test
      summary: Test
      description: Test.
      operationId: TestTest
      parameters:
        - name: parameters
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/TestRequestParameters"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/TestRequestParameters"
                title: TestRequestParameters
              - type: "null"
            description: Parameters for testing.
          description: Parameters for testing.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
      responses:
        "200":
          description: Request fulfilled, document follows
          headers: {}
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TestResponseResult"
        "400":
          description: Bad request syntax or unsupported method
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Validation Exception
                examples:
                  - status_code: 400
                    detail: Bad Request
                    extra: {}
      deprecated: false
components:
  schemas:
    CreateResponseEvent:
      $ref: "#/components/schemas/events_models_Event"
    CreateResponseInstance:
      properties:
        start:
          $ref: "#/components/schemas/instances_models_NaiveDatetime"
        duration:
          $ref: "#/components/schemas/instances_models_Timedelta"
        eventId:
          type: string
          format: uuid
          title: Eventid
          description: Identifier of the event the instance belongs to.
        event:
          anyOf:
            - $ref: "#/components/schemas/instances_models_Event"
            - type: "null"
          description: Event the instance belongs to.
      type: object
      required:
        - start
        - duration
        - eventId
        - event
      title: Instance
      description: Instance data.
    CreateResponseShow:
      $ref: "#/components/schemas/shows_models_Show"
    DeleteRequestEventId:
      type: string
      format: uuid
    DeleteRequestStart:
      type: string
      format: date-time
      description: Datetime without timezone.
      examples:
        - 2000-01-01T00:00:00
    EventArgsFromInstance:
      properties:
        include:
          $ref: "#/components/schemas/instances_models_EventInclude"
      type: object
      title: EventArgsFromInstance
      description: Event arguments to include when querying instances.
    EventOrderByEndInput:
      properties:
        end:
          type: string
          enum:
            - asc
            - desc
          title: End
      type: object
      required:
        - end
      title: EventOrderByEndInput
      description: Order by end time.
    EventOrderByInput:
      anyOf:
        - $ref: "#/components/schemas/events_models__Event_id_OrderByInput"
        - $ref: "#/components/schemas/events_models__Event_type_OrderByInput"
        - $ref: "#/components/schemas/events_models__Event_showId_OrderByInput"
        - $ref: "#/components/schemas/EventOrderByStartInput"
        - $ref: "#/components/schemas/EventOrderByEndInput"
        - $ref: "#/components/schemas/EventOrderByTimezoneInput"
    EventOrderByStartInput:
      properties:
        start:
          type: string
          enum:
            - asc
            - desc
          title: Start
      type: object
      required:
        - start
      title: EventOrderByStartInput
      description: Order by start time.
    EventOrderByTimezoneInput:
      properties:
        timezone:
          type: string
          enum:
            - asc
            - desc
          title: Timezone
      type: object
      required:
        - timezone
      title: EventOrderByTimezoneInput
      description: Order by timezone.
    EventRelationFilter:
      properties:
        is:
          $ref: "#/components/schemas/instances_models_EventWhereInput"
        isNot:
          $ref: "#/components/schemas/instances_models_EventWhereInput"
      type: object
      title: EventRelationFilter
    EventUpdateInput:
      properties:
        id:
          type: string
          title: Id
        type:
          $ref: "#/components/schemas/events_models_EventType"
        start:
          $ref: "#/components/schemas/events_models_NaiveDatetime"
        duration:
          $ref: "#/components/schemas/events_models_Timedelta"
        timezone:
          $ref: "#/components/schemas/events_models_Timezone"
        recurrence:
          anyOf:
            - $ref: "#/components/schemas/RecurrenceUpdateInput"
            - type: "null"
          description: Recurrence rule of the event.
        include:
          anyOf:
            - items:
                $ref: "#/components/schemas/events_models_Inclusion"
              type: array
              uniqueItems: true
            - type: "null"
          title: Include
          description: Included instances of the event.
        exclude:
          anyOf:
            - items:
                $ref: "#/components/schemas/events_models_Exclusion"
              type: array
              uniqueItems: true
            - type: "null"
          title: Exclude
          description: Excluded instances of the event.
      type: object
      title: EventUpdateInput
      description: Data to update an event.
    GetRequestEventId:
      type: string
      format: uuid
    GetRequestStart:
      type: string
      format: date-time
      description: Datetime without timezone.
      examples:
        - 2000-01-01T00:00:00
    GetResponseEvent:
      properties:
        id:
          type: string
          format: uuid
          title: Id
          description: Identifier of the event.
        type:
          $ref: "#/components/schemas/events_models_EventType"
        showId:
          anyOf:
            - type: string
              format: uuid
            - type: "null"
          title: Showid
          description: Identifier of the show the event belongs to.
        show:
          anyOf:
            - $ref: "#/components/schemas/events_models_Show"
            - type: "null"
          description: Show the event belongs to.
        start:
          $ref: "#/components/schemas/events_models_NaiveDatetime"
        duration:
          $ref: "#/components/schemas/events_models_Timedelta"
        timezone:
          $ref: "#/components/schemas/events_models_Timezone"
        recurrence:
          anyOf:
            - $ref: "#/components/schemas/events_models_Recurrence"
            - type: "null"
          description: Recurrence rule of the event.
          default: null
        include:
          anyOf:
            - items:
                $ref: "#/components/schemas/events_models_Inclusion"
              type: array
              uniqueItems: true
            - type: "null"
          title: Include
          description: Included instances of the event.
          default: null
        exclude:
          anyOf:
            - items:
                $ref: "#/components/schemas/events_models_Exclusion"
              type: array
              uniqueItems: true
            - type: "null"
          title: Exclude
          description: Excluded instances of the event.
          default: null
      type: object
      required:
        - id
        - type
        - showId
        - show
        - start
        - duration
        - timezone
      title: Event
      description: Event data.
    GetResponseInstance:
      anyOf:
        - $ref: "#/components/schemas/Instance"
        - type: "null"
    GetResponseShow:
      properties:
        id:
          type: string
          format: uuid
          title: Id
          description: Identifier of the show.
        title:
          type: string
          title: Title
          description: Title of the show.
        description:
          anyOf:
            - type: string
            - type: "null"
          title: Description
          description: Description of the show.
        events:
          anyOf:
            - items:
                $ref: "#/components/schemas/shows_models_Event"
              type: array
            - type: "null"
          title: Events
          description: Events the show belongs to.
      type: object
      required:
        - id
        - title
        - description
        - events
      title: Show
      description: Show data.
    Instance:
      properties:
        start:
          $ref: "#/components/schemas/instances_models_NaiveDatetime"
        duration:
          $ref: "#/components/schemas/instances_models_Timedelta"
        eventId:
          type: string
          format: uuid
          title: Eventid
          description: Identifier of the event the instance belongs to.
        event:
          anyOf:
            - $ref: "#/components/schemas/instances_models_Event"
            - type: "null"
          description: Event the instance belongs to.
      type: object
      required:
        - start
        - duration
        - eventId
        - event
      title: Instance
      description: Instance data.
    InstanceInclude:
      properties:
        event:
          anyOf:
            - type: boolean
            - $ref: "#/components/schemas/EventArgsFromInstance"
          title: Event
          description: Event relation to include.
      type: object
      title: InstanceInclude
      description: Relations to include when querying instances.
    InstanceOrderByDurationInput:
      properties:
        duration:
          $ref: "#/components/schemas/SortOrder"
      type: object
      required:
        - duration
      title: InstanceOrderByDurationInput
      description: Order by duration of the instance.
    InstanceOrderByEventIdInput:
      properties:
        eventId:
          $ref: "#/components/schemas/SortOrder"
      type: object
      required:
        - eventId
      title: InstanceOrderByEventIdInput
      description: Order by event ID of the instance.
    InstanceOrderByInput:
      anyOf:
        - $ref: "#/components/schemas/InstanceOrderByStartInput"
        - $ref: "#/components/schemas/InstanceOrderByDurationInput"
        - $ref: "#/components/schemas/InstanceOrderByEventIdInput"
    InstanceOrderByStartInput:
      properties:
        start:
          $ref: "#/components/schemas/SortOrder"
      type: object
      required:
        - start
      title: InstanceOrderByStartInput
      description: Order by start datetime of the instance.
    InstanceWhereInput:
      properties:
        event:
          $ref: "#/components/schemas/EventRelationFilter"
      type: object
      title: InstanceWhereInput
      description: Instance arguments for searching.
    ListRequestEnd:
      type: string
      format: date-time
      description: Datetime in UTC.
      examples:
        - 2000-01-01T00:00:00Z
    ListRequestQuery:
      anyOf:
        - $ref: "#/components/schemas/Query"
        - type: "null"
    ListRequestStart:
      type: string
      format: date-time
      description: Datetime in UTC.
      examples:
        - 2000-01-01T00:00:00Z
    Parameters:
      properties:
        message:
          anyOf:
            - type: string
            - type: "null"
          title: Message
          description: Optional message to include in the test.
          default: null
      type: object
      title: Parameters
      description: Parameters for testing.
    Query:
      oneOf:
        - $ref: "#/components/schemas/TimeRangeQuery"
        - $ref: "#/components/schemas/RecurringQuery"
      discriminator:
        propertyName: type
        mapping:
          recurring: "#/components/schemas/beaver_api_routes_events_models_RecurringQuery"
          time-range: "#/components/schemas/beaver_api_routes_events_models_TimeRangeQuery"
    RecurrenceUpdateInput:
      properties:
        frequency:
          $ref: "#/components/schemas/events_models_Frequency"
        termination:
          anyOf:
            - $ref: "#/components/schemas/events_models_Termination"
            - type: "null"
          description: Termination of the recurrence.
        interval:
          anyOf:
            - type: integer
              exclusiveMinimum: 0
            - type: "null"
          title: Interval
          description: Interval of the recurrence.
        bySeconds:
          anyOf:
            - items:
                $ref: "#/components/schemas/events_models_Second"
              type: array
              uniqueItems: true
            - type: "null"
          title: Byseconds
          description: Seconds of the recurrence.
        byMinutes:
          anyOf:
            - items:
                $ref: "#/components/schemas/events_models_Minute"
              type: array
              uniqueItems: true
            - type: "null"
          title: Byminutes
          description: Minutes of the recurrence.
        byHours:
          anyOf:
            - items:
                $ref: "#/components/schemas/events_models_Hour"
              type: array
              uniqueItems: true
            - type: "null"
          title: Byhours
          description: Hours of the recurrence.
        byWeekdays:
          anyOf:
            - items:
                $ref: "#/components/schemas/events_models_WeekdayRule"
              type: array
              uniqueItems: true
            - type: "null"
          title: Byweekdays
          description: Weekdays of the recurrence.
        byMonthdays:
          anyOf:
            - items:
                $ref: "#/components/schemas/events_models_Monthday"
              type: array
              uniqueItems: true
            - type: "null"
          title: Bymonthdays
          description: Monthdays of the recurrence.
        byYeardays:
          anyOf:
            - items:
                $ref: "#/components/schemas/events_models_Yearday"
              type: array
              uniqueItems: true
            - type: "null"
          title: Byyeardays
          description: Yeardays of the recurrence.
        byWeeks:
          anyOf:
            - items:
                $ref: "#/components/schemas/events_models_Week"
              type: array
              uniqueItems: true
            - type: "null"
          title: Byweeks
          description: Weeks of the recurrence.
        byMonths:
          anyOf:
            - items:
                $ref: "#/components/schemas/events_models_Month"
              type: array
              uniqueItems: true
            - type: "null"
          title: Bymonths
          description: Months of the recurrence.
        bySetPositions:
          anyOf:
            - items:
                $ref: "#/components/schemas/events_models_Yearday"
              type: array
              uniqueItems: true
            - type: "null"
          title: Bysetpositions
          description: Set positions of the recurrence.
        weekStart:
          anyOf:
            - $ref: "#/components/schemas/events_models_Weekday"
            - type: "null"
          description: Start day of the week.
      type: object
      title: RecurrenceUpdateInput
      description: Data to update a recurrence rule.
    RecurringQuery:
      properties:
        type:
          type: string
          const: recurring
          title: Type
          description: Type of the query.
          default: recurring
        recurring:
          type: boolean
          title: Recurring
          description: Whether to search for recurring or non-recurring events.
      type: object
      required:
        - recurring
      title: RecurringQuery
      description: Query for recurring events.
    ShowInclude:
      properties:
        events:
          anyOf:
            - type: boolean
            - $ref: "#/components/schemas/shows_models_FindManyEventArgsFromShow"
          title: Events
      type: object
      title: ShowInclude
      description: Show relational arguments
    ShowOrderByInput:
      anyOf:
        - $ref: "#/components/schemas/_Show_id_OrderByInput"
        - $ref: "#/components/schemas/_Show_title_OrderByInput"
        - $ref: "#/components/schemas/_Show_description_OrderByInput"
    SortOrder:
      type: string
      enum:
        - asc
        - desc
    SplitRequestData:
      properties:
        at:
          $ref: "#/components/schemas/events_models_NaiveDatetime"
        update:
          anyOf:
            - $ref: "#/components/schemas/EventUpdateInput"
            - type: "null"
          description: Data to update the event after the split.
      type: object
      required:
        - at
      title: EventSplitInput
      description: Data to split an event.
    SplitRequestId:
      type: string
      format: uuid
    SplitRequestInclude:
      anyOf:
        - $ref: "#/components/schemas/events_models_EventInclude"
        - type: "null"
    SplitResponseResult:
      properties:
        before:
          $ref: "#/components/schemas/GetResponseEvent"
        after:
          $ref: "#/components/schemas/events_models_Event"
      type: object
      required:
        - before
        - after
      title: SplitResult
      description: Result of splitting an event.
    SubscribeRequestTypes:
      anyOf:
        - type: string
        - items:
            $ref: "#/components/schemas/sse_models_EventType"
          type: array
          uniqueItems: true
        - type: "null"
    TestRequestParameters:
      anyOf:
        - $ref: "#/components/schemas/Parameters"
        - type: "null"
    TestResponseResult:
      properties:
        success:
          type: boolean
          title: Success
          description: Indicates if the test was successful.
        message:
          type: string
          title: Message
          description: Message returned from the test.
      type: object
      required:
        - success
        - message
      title: Result
      description: Result of the test.
    TimeRangeQuery:
      properties:
        type:
          type: string
          const: time-range
          title: Type
          description: Type of the query.
          default: time-range
        start:
          anyOf:
            - $ref: "#/components/schemas/events_models_UTCDatetime"
            - type: "null"
          description: Beginning of the time range in UTC.
          default: null
        end:
          anyOf:
            - $ref: "#/components/schemas/events_models_UTCDatetime"
            - type: "null"
          description: End of the time range in UTC.
          default: null
      type: object
      title: TimeRangeQuery
      description: Query for events in a time range.
    UpdateRequestEventId:
      type: string
      format: uuid
    UpdateRequestStart:
      type: string
      format: date-time
      description: Datetime without timezone.
      examples:
        - 2000-01-01T00:00:00
    UpdateResponseEvent:
      $ref: "#/components/schemas/events_models_Event"
    UpdateResponseInstance:
      properties:
        start:
          $ref: "#/components/schemas/instances_models_NaiveDatetime"
        duration:
          $ref: "#/components/schemas/instances_models_Timedelta"
        eventId:
          type: string
          format: uuid
          title: Eventid
          description: Identifier of the event the instance belongs to.
        event:
          anyOf:
            - $ref: "#/components/schemas/instances_models_Event"
            - type: "null"
          description: Event the instance belongs to.
      type: object
      required:
        - start
        - duration
        - eventId
        - event
      title: Instance
      description: Instance data.
    UpdateResponseShow:
      $ref: "#/components/schemas/shows_models_Show"
    _Show_description_OrderByInput:
      properties:
        description:
          type: string
          enum:
            - asc
            - desc
          title: Description
      type: object
      required:
        - description
      title: _Show_description_OrderByInput
    _Show_id_OrderByInput:
      properties:
        id:
          type: string
          enum:
            - asc
            - desc
          title: Id
      type: object
      required:
        - id
      title: _Show_id_OrderByInput
    _Show_title_OrderByInput:
      properties:
        title:
          type: string
          enum:
            - asc
            - desc
          title: Title
      type: object
      required:
        - title
      title: _Show_title_OrderByInput
    events_models_CountTermination:
      properties:
        type:
          type: string
          const: count
          title: Type
          description: Type of the termination.
          default: count
        count:
          type: integer
          exclusiveMinimum: 0
          title: Count
          description: Number of instances of recurring event.
      type: object
      required:
        - count
      title: CountTermination
      description: Count termination data.
    events_models_CreateRequestData:
      properties:
        id:
          type: string
          title: Id
        showId:
          anyOf:
            - type: string
            - type: "null"
          title: Showid
        type:
          $ref: "#/components/schemas/events_models_EventType"
        start:
          $ref: "#/components/schemas/events_models_NaiveDatetime"
        duration:
          $ref: "#/components/schemas/events_models_Timedelta"
        timezone:
          $ref: "#/components/schemas/events_models_Timezone"
        recurrence:
          anyOf:
            - $ref: "#/components/schemas/events_models_Recurrence"
            - type: "null"
          description: Recurrence rule of the event.
        include:
          anyOf:
            - items:
                $ref: "#/components/schemas/events_models_Inclusion"
              type: array
              uniqueItems: true
            - type: "null"
          title: Include
          description: Included instances of the event.
        exclude:
          anyOf:
            - items:
                $ref: "#/components/schemas/events_models_Exclusion"
              type: array
              uniqueItems: true
            - type: "null"
          title: Exclude
          description: Excluded instances of the event.
      type: object
      required:
        - type
        - start
        - duration
        - timezone
      title: EventCreateInput
      description: Data to create an event.
    events_models_CreateRequestInclude:
      anyOf:
        - $ref: "#/components/schemas/events_models_EventInclude"
        - type: "null"
    events_models_DeleteRequestId:
      type: string
      format: uuid
    events_models_Event:
      properties:
        id:
          type: string
          format: uuid
          title: Id
          description: Identifier of the event.
        type:
          $ref: "#/components/schemas/events_models_EventType"
        showId:
          anyOf:
            - type: string
              format: uuid
            - type: "null"
          title: Showid
          description: Identifier of the show the event belongs to.
        show:
          anyOf:
            - $ref: "#/components/schemas/events_models_Show"
            - type: "null"
          description: Show the event belongs to.
        start:
          $ref: "#/components/schemas/events_models_NaiveDatetime"
        duration:
          $ref: "#/components/schemas/events_models_Timedelta"
        timezone:
          $ref: "#/components/schemas/events_models_Timezone"
        recurrence:
          anyOf:
            - $ref: "#/components/schemas/events_models_Recurrence"
            - type: "null"
          description: Recurrence rule of the event.
          default: null
        include:
          anyOf:
            - items:
                $ref: "#/components/schemas/events_models_Inclusion"
              type: array
              uniqueItems: true
            - type: "null"
          title: Include
          description: Included instances of the event.
          default: null
        exclude:
          anyOf:
            - items:
                $ref: "#/components/schemas/events_models_Exclusion"
              type: array
              uniqueItems: true
            - type: "null"
          title: Exclude
          description: Excluded instances of the event.
          default: null
      type: object
      required:
        - id
        - type
        - showId
        - show
        - start
        - duration
        - timezone
      title: Event
      description: Event data.
    events_models_EventInclude:
      properties:
        show:
          anyOf:
            - type: boolean
            - $ref: "#/components/schemas/events_models_ShowArgsFromEvent"
          title: Show
      type: object
      title: EventInclude
      description: Event relational arguments
    events_models_EventIncludeFromEvent:
      properties:
        show:
          anyOf:
            - type: boolean
            - $ref: "#/components/schemas/events_models_ShowArgsFromEvent"
          title: Show
      type: object
      title: EventIncludeFromEvent
      description: Relational arguments for Event
    events_models_EventListRelationFilter:
      properties:
        some:
          $ref: "#/components/schemas/events_models_EventWhereInput"
        none:
          $ref: "#/components/schemas/events_models_EventWhereInput"
        every:
          $ref: "#/components/schemas/events_models_EventWhereInput"
      type: object
      title: EventListRelationFilter
    events_models_EventType:
      type: string
      enum:
        - live
        - replay
        - prerecorded
      title: EventType
    events_models_EventWhereInput:
      properties:
        id:
          anyOf:
            - type: string
            - $ref: "#/components/schemas/events_models_StringFilter"
          title: Id
        type:
          $ref: "#/components/schemas/events_models_EventType"
        showId:
          anyOf:
            - type: string
            - $ref: "#/components/schemas/events_models_StringFilter"
            - type: "null"
          title: Showid
        show:
          $ref: "#/components/schemas/events_models_ShowRelationFilter"
        and:
          items:
            $ref: "#/components/schemas/events_models_EventWhereInput"
          type: array
          title: And
        or:
          items:
            $ref: "#/components/schemas/events_models_EventWhereInput"
          type: array
          title: Or
        not:
          items:
            $ref: "#/components/schemas/events_models_EventWhereInput"
          type: array
          title: Not
      type: object
      title: EventWhereInput
      description: Event arguments for searching
    events_models_Exclusion:
      properties:
        start:
          $ref: "#/components/schemas/events_models_NaiveDatetime"
      type: object
      required:
        - start
      title: Exclusion
      description: Exclusion data.
    events_models_FindManyEventArgsFromShow:
      properties:
        take:
          type: integer
          title: Take
        skip:
          type: integer
          title: Skip
        orderBy:
          anyOf:
            - $ref: "#/components/schemas/events_models__Event_id_OrderByInput"
            - $ref: "#/components/schemas/events_models__Event_type_OrderByInput"
            - $ref: "#/components/schemas/events_models__Event_showId_OrderByInput"
            - $ref: "#/components/schemas/events_models__Event_RelevanceOrderByInput"
            - items:
                anyOf:
                  - $ref: "#/components/schemas/events_models__Event_id_OrderByInput"
                  - $ref: "#/components/schemas/events_models__Event_type_OrderByInput"
                  - $ref: "#/components/schemas/events_models__Event_showId_OrderByInput"
                  - $ref: "#/components/schemas/events_models__Event_RelevanceOrderByInput"
              type: array
          title: Orderby
        where:
          $ref: "#/components/schemas/events_models_EventWhereInput"
        cursor:
          $ref: "#/components/schemas/events_models__EventWhereUnique_id_Input"
        distinct:
          items:
            type: string
            enum:
              - id
              - type
              - showId
          type: array
          title: Distinct
        include:
          $ref: "#/components/schemas/events_models_EventIncludeFromEvent"
      type: object
      title: FindManyEventArgsFromShow
      description: Arguments for Show
    events_models_Frequency:
      type: string
      enum:
        - secondly
        - minutely
        - hourly
        - daily
        - weekly
        - monthly
        - yearly
      title: Frequency
      description: Frequency options.
    events_models_GetRequestId:
      type: string
      format: uuid
    events_models_GetRequestInclude:
      anyOf:
        - $ref: "#/components/schemas/events_models_EventInclude"
        - type: "null"
    events_models_Hour:
      type: integer
      maximum: 23
      minimum: 0
    events_models_Inclusion:
      properties:
        start:
          $ref: "#/components/schemas/events_models_NaiveDatetime"
      type: object
      required:
        - start
      title: Inclusion
      description: Inclusion data.
    events_models_ListRequestInclude:
      anyOf:
        - $ref: "#/components/schemas/events_models_EventInclude"
        - type: "null"
    events_models_ListRequestLimit:
      anyOf:
        - type: integer
        - type: "null"
    events_models_ListRequestOffset:
      anyOf:
        - type: integer
        - type: "null"
    events_models_ListRequestOrder:
      anyOf:
        - $ref: "#/components/schemas/EventOrderByInput"
        - items:
            $ref: "#/components/schemas/EventOrderByInput"
          type: array
        - type: "null"
    events_models_ListRequestWhere:
      anyOf:
        - $ref: "#/components/schemas/events_models_EventWhereInput"
        - type: "null"
    events_models_ListResponseResults:
      properties:
        count:
          type: integer
          title: Count
          description: Total number of events that matched the query.
        limit:
          anyOf:
            - type: integer
            - type: "null"
          title: Limit
          description: Maximum number of returned events.
        offset:
          anyOf:
            - type: integer
            - type: "null"
          title: Offset
          description: Number of events skipped.
        events:
          items:
            $ref: "#/components/schemas/events_models_Event"
          type: array
          title: Events
          description: Events that matched the request.
      type: object
      required:
        - count
        - limit
        - offset
        - events
      title: EventList
      description: List of events.
    events_models_Minute:
      type: integer
      maximum: 59
      minimum: 0
    events_models_Month:
      type: integer
      maximum: 12
      minimum: 1
    events_models_Monthday:
      anyOf:
        - type: integer
          maximum: -1
          minimum: -31
        - type: integer
          maximum: 31
          minimum: 1
    events_models_NaiveDatetime:
      type: string
      format: date-time
      description: Datetime without timezone.
      examples:
        - 2000-01-01T00:00:00
    events_models_Recurrence:
      properties:
        frequency:
          $ref: "#/components/schemas/events_models_Frequency"
        termination:
          anyOf:
            - $ref: "#/components/schemas/events_models_Termination"
            - type: "null"
          description: Termination of the recurrence.
          default: null
        interval:
          anyOf:
            - type: integer
              exclusiveMinimum: 0
            - type: "null"
          title: Interval
          description: Interval of the recurrence.
          default: null
        bySeconds:
          anyOf:
            - items:
                $ref: "#/components/schemas/events_models_Second"
              type: array
              uniqueItems: true
            - type: "null"
          title: Byseconds
          description: Seconds of the recurrence.
          default: null
        byMinutes:
          anyOf:
            - items:
                $ref: "#/components/schemas/events_models_Minute"
              type: array
              uniqueItems: true
            - type: "null"
          title: Byminutes
          description: Minutes of the recurrence.
          default: null
        byHours:
          anyOf:
            - items:
                $ref: "#/components/schemas/events_models_Hour"
              type: array
              uniqueItems: true
            - type: "null"
          title: Byhours
          description: Hours of the recurrence.
          default: null
        byWeekdays:
          anyOf:
            - items:
                $ref: "#/components/schemas/events_models_WeekdayRule"
              type: array
              uniqueItems: true
            - type: "null"
          title: Byweekdays
          description: Weekdays of the recurrence.
          default: null
        byMonthdays:
          anyOf:
            - items:
                $ref: "#/components/schemas/events_models_Monthday"
              type: array
              uniqueItems: true
            - type: "null"
          title: Bymonthdays
          description: Monthdays of the recurrence.
          default: null
        byYeardays:
          anyOf:
            - items:
                $ref: "#/components/schemas/events_models_Yearday"
              type: array
              uniqueItems: true
            - type: "null"
          title: Byyeardays
          description: Yeardays of the recurrence.
          default: null
        byWeeks:
          anyOf:
            - items:
                $ref: "#/components/schemas/events_models_Week"
              type: array
              uniqueItems: true
            - type: "null"
          title: Byweeks
          description: Weeks of the recurrence.
          default: null
        byMonths:
          anyOf:
            - items:
                $ref: "#/components/schemas/events_models_Month"
              type: array
              uniqueItems: true
            - type: "null"
          title: Bymonths
          description: Months of the recurrence.
          default: null
        bySetPositions:
          anyOf:
            - items:
                $ref: "#/components/schemas/events_models_Yearday"
              type: array
              uniqueItems: true
            - type: "null"
          title: Bysetpositions
          description: Set positions of the recurrence.
          default: null
        weekStart:
          anyOf:
            - $ref: "#/components/schemas/events_models_Weekday"
            - type: "null"
          description: Start day of the week.
          default: null
      type: object
      required:
        - frequency
      title: Recurrence
      description: Recurrence rule data.
    events_models_Second:
      type: integer
      maximum: 59
      minimum: 0
    events_models_Show:
      properties:
        id:
          type: string
          title: Id
          description: Identifier of the show.
        title:
          type: string
          title: Title
          description: Title of the show.
        description:
          anyOf:
            - type: string
            - type: "null"
          title: Description
          description: Description of the show.
        events:
          anyOf:
            - items:
                $ref: "#/components/schemas/events_models_Event"
              type: array
            - type: "null"
          title: Events
          description: Events the show belongs to.
      type: object
      required:
        - id
        - title
        - description
        - events
      title: Show
      description: Show data.
    events_models_ShowArgsFromEvent:
      properties:
        include:
          $ref: "#/components/schemas/events_models_ShowIncludeFromShow"
      type: object
      title: ShowArgsFromEvent
      description: Arguments for Event
    events_models_ShowIncludeFromShow:
      properties:
        events:
          anyOf:
            - type: boolean
            - $ref: "#/components/schemas/events_models_FindManyEventArgsFromShow"
          title: Events
      type: object
      title: ShowIncludeFromShow
      description: Relational arguments for Show
    events_models_ShowRelationFilter:
      properties:
        is:
          $ref: "#/components/schemas/events_models_ShowWhereInput"
        isNot:
          $ref: "#/components/schemas/events_models_ShowWhereInput"
      type: object
      title: ShowRelationFilter
    events_models_ShowWhereInput:
      properties:
        id:
          anyOf:
            - type: string
            - $ref: "#/components/schemas/events_models_StringFilter"
          title: Id
        title:
          anyOf:
            - type: string
            - $ref: "#/components/schemas/events_models_StringFilter"
          title: Title
        description:
          anyOf:
            - type: string
            - $ref: "#/components/schemas/events_models_StringFilter"
            - type: "null"
          title: Description
        events:
          $ref: "#/components/schemas/events_models_EventListRelationFilter"
        and:
          items:
            $ref: "#/components/schemas/events_models_ShowWhereInput"
          type: array
          title: And
        or:
          items:
            $ref: "#/components/schemas/events_models_ShowWhereInput"
          type: array
          title: Or
        not:
          items:
            $ref: "#/components/schemas/events_models_ShowWhereInput"
          type: array
          title: Not
      type: object
      title: ShowWhereInput
      description: Show arguments for searching
    events_models_StringFilter:
      properties:
        equals:
          type: string
          title: Equals
        notIn:
          items:
            type: string
          type: array
          title: Notin
        lt:
          type: string
          title: Lt
        lte:
          type: string
          title: Lte
        gt:
          type: string
          title: Gt
        gte:
          type: string
          title: Gte
        contains:
          type: string
          title: Contains
        startswith:
          type: string
          title: Startswith
        endswith:
          type: string
          title: Endswith
        in:
          items:
            type: string
          type: array
          title: In
        not:
          anyOf:
            - type: string
            - $ref: "#/components/schemas/events_models_StringFilter"
          title: Not
        mode:
          type: string
          enum:
            - default
            - insensitive
          title: Mode
        search:
          type: string
          title: Search
      type: object
      title: StringFilter
    events_models_Termination:
      oneOf:
        - $ref: "#/components/schemas/events_models_CountTermination"
        - $ref: "#/components/schemas/events_models_UntilTermination"
      discriminator:
        propertyName: type
        mapping:
          count: "#/components/schemas/beaver_api_routes_events_models_CountTermination"
          until: "#/components/schemas/beaver_api_routes_events_models_UntilTermination"
    events_models_Timedelta:
      type: string
      format: duration
      description: Duration of time.
      examples:
        - PT1H
    events_models_Timezone:
      type: string
      description: Timezone name.
      examples:
        - Europe/Warsaw
    events_models_UTCDatetime:
      type: string
      format: date-time
      description: Datetime in UTC.
      examples:
        - 2000-01-01T00:00:00Z
    events_models_UntilTermination:
      properties:
        type:
          type: string
          const: until
          title: Type
          description: Type of the termination.
          default: until
        until:
          $ref: "#/components/schemas/events_models_NaiveDatetime"
      type: object
      required:
        - until
      title: UntilTermination
      description: Until termination data.
    events_models_UpdateRequestData:
      properties:
        id:
          type: string
          title: Id
        type:
          $ref: "#/components/schemas/events_models_EventType"
        start:
          $ref: "#/components/schemas/events_models_NaiveDatetime"
        duration:
          $ref: "#/components/schemas/events_models_Timedelta"
        timezone:
          $ref: "#/components/schemas/events_models_Timezone"
        recurrence:
          anyOf:
            - $ref: "#/components/schemas/RecurrenceUpdateInput"
            - type: "null"
          description: Recurrence rule of the event.
        include:
          anyOf:
            - items:
                $ref: "#/components/schemas/events_models_Inclusion"
              type: array
              uniqueItems: true
            - type: "null"
          title: Include
          description: Included instances of the event.
        exclude:
          anyOf:
            - items:
                $ref: "#/components/schemas/events_models_Exclusion"
              type: array
              uniqueItems: true
            - type: "null"
          title: Exclude
          description: Excluded instances of the event.
      type: object
      title: EventUpdateInput
      description: Data to update an event.
    events_models_UpdateRequestId:
      type: string
      format: uuid
    events_models_UpdateRequestInclude:
      anyOf:
        - $ref: "#/components/schemas/events_models_EventInclude"
        - type: "null"
    events_models_Week:
      anyOf:
        - type: integer
          maximum: -1
          minimum: -53
        - type: integer
          maximum: 53
          minimum: 1
    events_models_Weekday:
      type: string
      enum:
        - monday
        - tuesday
        - wednesday
        - thursday
        - friday
        - saturday
        - sunday
      title: Weekday
      description: Weekday options.
    events_models_WeekdayRule:
      properties:
        day:
          $ref: "#/components/schemas/events_models_Weekday"
        occurrence:
          anyOf:
            - $ref: "#/components/schemas/events_models_Week"
            - type: "null"
          description: Occurrence of the day in the year.
          default: null
      type: object
      required:
        - day
      title: WeekdayRule
      description: Day rule data.
    events_models_Yearday:
      anyOf:
        - type: integer
          maximum: -1
          minimum: -366
        - type: integer
          maximum: 366
          minimum: 1
    events_models__EventWhereUnique_id_Input:
      properties:
        id:
          type: string
          title: Id
      type: object
      required:
        - id
      title: _EventWhereUnique_id_Input
    events_models__Event_RelevanceInner:
      properties:
        fields:
          items:
            type: string
            enum:
              - id
              - type
              - showId
          type: array
          title: Fields
        search:
          type: string
          title: Search
        sort:
          type: string
          enum:
            - asc
            - desc
          title: Sort
      type: object
      required:
        - fields
        - search
        - sort
      title: _Event_RelevanceInner
    events_models__Event_RelevanceOrderByInput:
      properties:
        _relevance:
          $ref: "#/components/schemas/events_models__Event_RelevanceInner"
      type: object
      required:
        - _relevance
      title: _Event_RelevanceOrderByInput
    events_models__Event_id_OrderByInput:
      properties:
        id:
          type: string
          enum:
            - asc
            - desc
          title: Id
      type: object
      required:
        - id
      title: _Event_id_OrderByInput
    events_models__Event_showId_OrderByInput:
      properties:
        showId:
          type: string
          enum:
            - asc
            - desc
          title: Showid
      type: object
      required:
        - showId
      title: _Event_showId_OrderByInput
    events_models__Event_type_OrderByInput:
      properties:
        type:
          type: string
          enum:
            - asc
            - desc
          title: Type
      type: object
      required:
        - type
      title: _Event_type_OrderByInput
    instances_models_CountTermination:
      properties:
        type:
          type: string
          const: count
          title: Type
          description: Type of the termination.
          default: count
        count:
          type: integer
          exclusiveMinimum: 0
          title: Count
          description: Number of instances of recurring event.
      type: object
      required:
        - count
      title: CountTermination
      description: Count termination data.
    instances_models_CreateRequestData:
      properties:
        start:
          $ref: "#/components/schemas/instances_models_NaiveDatetime"
        eventId:
          type: string
          title: Eventid
          description: Identifier of the event the instance belongs to.
      type: object
      required:
        - start
        - eventId
      title: InstanceCreateInput
      description: Data to create an instance.
    instances_models_CreateRequestInclude:
      anyOf:
        - $ref: "#/components/schemas/InstanceInclude"
        - type: "null"
    instances_models_Event:
      properties:
        id:
          type: string
          format: uuid
          title: Id
          description: Identifier of the event.
        type:
          $ref: "#/components/schemas/instances_models_EventType"
        showId:
          anyOf:
            - type: string
              format: uuid
            - type: "null"
          title: Showid
          description: Identifier of the show the event belongs to.
        show:
          anyOf:
            - $ref: "#/components/schemas/instances_models_Show"
            - type: "null"
          description: Show the event belongs to.
        start:
          $ref: "#/components/schemas/instances_models_NaiveDatetime"
        duration:
          $ref: "#/components/schemas/instances_models_Timedelta"
        timezone:
          $ref: "#/components/schemas/instances_models_Timezone"
        recurrence:
          anyOf:
            - $ref: "#/components/schemas/instances_models_Recurrence"
            - type: "null"
          description: Recurrence rule of the event.
          default: null
        include:
          anyOf:
            - items:
                $ref: "#/components/schemas/instances_models_Inclusion"
              type: array
              uniqueItems: true
            - type: "null"
          title: Include
          description: Included instances of the event.
          default: null
        exclude:
          anyOf:
            - items:
                $ref: "#/components/schemas/instances_models_Exclusion"
              type: array
              uniqueItems: true
            - type: "null"
          title: Exclude
          description: Excluded instances of the event.
          default: null
      type: object
      required:
        - id
        - type
        - showId
        - show
        - start
        - duration
        - timezone
      title: Event
      description: Event data.
    instances_models_EventInclude:
      properties:
        show:
          anyOf:
            - type: boolean
            - $ref: "#/components/schemas/instances_models_ShowArgsFromEvent"
          title: Show
      type: object
      title: EventInclude
      description: Event relational arguments
    instances_models_EventIncludeFromEvent:
      properties:
        show:
          anyOf:
            - type: boolean
            - $ref: "#/components/schemas/instances_models_ShowArgsFromEvent"
          title: Show
      type: object
      title: EventIncludeFromEvent
      description: Relational arguments for Event
    instances_models_EventListRelationFilter:
      properties:
        some:
          $ref: "#/components/schemas/instances_models_EventWhereInput"
        none:
          $ref: "#/components/schemas/instances_models_EventWhereInput"
        every:
          $ref: "#/components/schemas/instances_models_EventWhereInput"
      type: object
      title: EventListRelationFilter
    instances_models_EventType:
      type: string
      enum:
        - live
        - replay
        - prerecorded
      title: EventType
    instances_models_EventWhereInput:
      properties:
        id:
          anyOf:
            - type: string
            - $ref: "#/components/schemas/instances_models_StringFilter"
          title: Id
        type:
          $ref: "#/components/schemas/instances_models_EventType"
        showId:
          anyOf:
            - type: string
            - $ref: "#/components/schemas/instances_models_StringFilter"
            - type: "null"
          title: Showid
        show:
          $ref: "#/components/schemas/instances_models_ShowRelationFilter"
        and:
          items:
            $ref: "#/components/schemas/instances_models_EventWhereInput"
          type: array
          title: And
        or:
          items:
            $ref: "#/components/schemas/instances_models_EventWhereInput"
          type: array
          title: Or
        not:
          items:
            $ref: "#/components/schemas/instances_models_EventWhereInput"
          type: array
          title: Not
      type: object
      title: EventWhereInput
      description: Event arguments for searching
    instances_models_Exclusion:
      properties:
        start:
          $ref: "#/components/schemas/instances_models_NaiveDatetime"
      type: object
      required:
        - start
      title: Exclusion
      description: Exclusion data.
    instances_models_FindManyEventArgsFromShow:
      properties:
        take:
          type: integer
          title: Take
        skip:
          type: integer
          title: Skip
        orderBy:
          anyOf:
            - $ref: "#/components/schemas/instances_models__Event_id_OrderByInput"
            - $ref: "#/components/schemas/instances_models__Event_type_OrderByInput"
            - $ref: "#/components/schemas/instances_models__Event_showId_OrderByInput"
            - $ref: "#/components/schemas/instances_models__Event_RelevanceOrderByInput"
            - items:
                anyOf:
                  - $ref: "#/components/schemas/instances_models__Event_id_OrderByInput"
                  - $ref: "#/components/schemas/instances_models__Event_type_OrderByInput"
                  - $ref: "#/components/schemas/instances_models__Event_showId_OrderByInput"
                  - $ref: "#/components/schemas/instances_models__Event_RelevanceOrderByInput"
              type: array
          title: Orderby
        where:
          $ref: "#/components/schemas/instances_models_EventWhereInput"
        cursor:
          $ref: "#/components/schemas/instances_models__EventWhereUnique_id_Input"
        distinct:
          items:
            type: string
            enum:
              - id
              - type
              - showId
          type: array
          title: Distinct
        include:
          $ref: "#/components/schemas/instances_models_EventIncludeFromEvent"
      type: object
      title: FindManyEventArgsFromShow
      description: Arguments for Show
    instances_models_Frequency:
      type: string
      enum:
        - secondly
        - minutely
        - hourly
        - daily
        - weekly
        - monthly
        - yearly
      title: Frequency
      description: Frequency options.
    instances_models_GetRequestInclude:
      anyOf:
        - $ref: "#/components/schemas/InstanceInclude"
        - type: "null"
    instances_models_Hour:
      type: integer
      maximum: 23
      minimum: 0
    instances_models_Inclusion:
      properties:
        start:
          $ref: "#/components/schemas/instances_models_NaiveDatetime"
      type: object
      required:
        - start
      title: Inclusion
      description: Inclusion data.
    instances_models_ListRequestInclude:
      anyOf:
        - $ref: "#/components/schemas/InstanceInclude"
        - type: "null"
    instances_models_ListRequestOrder:
      anyOf:
        - $ref: "#/components/schemas/InstanceOrderByInput"
        - items:
            $ref: "#/components/schemas/InstanceOrderByInput"
          type: array
        - type: "null"
    instances_models_ListRequestWhere:
      anyOf:
        - $ref: "#/components/schemas/InstanceWhereInput"
        - type: "null"
    instances_models_ListResponseResults:
      properties:
        start:
          $ref: "#/components/schemas/instances_models_UTCDatetime"
        end:
          $ref: "#/components/schemas/instances_models_UTCDatetime"
        instances:
          items:
            $ref: "#/components/schemas/Instance"
          type: array
          title: Instances
          description: Instances that matched the request.
      type: object
      required:
        - start
        - end
        - instances
      title: InstanceList
      description: List of instances.
    instances_models_Minute:
      type: integer
      maximum: 59
      minimum: 0
    instances_models_Month:
      type: integer
      maximum: 12
      minimum: 1
    instances_models_Monthday:
      anyOf:
        - type: integer
          maximum: -1
          minimum: -31
        - type: integer
          maximum: 31
          minimum: 1
    instances_models_NaiveDatetime:
      type: string
      format: date-time
      description: Datetime without timezone.
      examples:
        - 2000-01-01T00:00:00
    instances_models_Recurrence:
      properties:
        frequency:
          $ref: "#/components/schemas/instances_models_Frequency"
        termination:
          anyOf:
            - $ref: "#/components/schemas/instances_models_Termination"
            - type: "null"
          description: Termination of the recurrence.
          default: null
        interval:
          anyOf:
            - type: integer
              exclusiveMinimum: 0
            - type: "null"
          title: Interval
          description: Interval of the recurrence.
          default: null
        bySeconds:
          anyOf:
            - items:
                $ref: "#/components/schemas/instances_models_Second"
              type: array
              uniqueItems: true
            - type: "null"
          title: Byseconds
          description: Seconds of the recurrence.
          default: null
        byMinutes:
          anyOf:
            - items:
                $ref: "#/components/schemas/instances_models_Minute"
              type: array
              uniqueItems: true
            - type: "null"
          title: Byminutes
          description: Minutes of the recurrence.
          default: null
        byHours:
          anyOf:
            - items:
                $ref: "#/components/schemas/instances_models_Hour"
              type: array
              uniqueItems: true
            - type: "null"
          title: Byhours
          description: Hours of the recurrence.
          default: null
        byWeekdays:
          anyOf:
            - items:
                $ref: "#/components/schemas/instances_models_WeekdayRule"
              type: array
              uniqueItems: true
            - type: "null"
          title: Byweekdays
          description: Weekdays of the recurrence.
          default: null
        byMonthdays:
          anyOf:
            - items:
                $ref: "#/components/schemas/instances_models_Monthday"
              type: array
              uniqueItems: true
            - type: "null"
          title: Bymonthdays
          description: Monthdays of the recurrence.
          default: null
        byYeardays:
          anyOf:
            - items:
                $ref: "#/components/schemas/instances_models_Yearday"
              type: array
              uniqueItems: true
            - type: "null"
          title: Byyeardays
          description: Yeardays of the recurrence.
          default: null
        byWeeks:
          anyOf:
            - items:
                $ref: "#/components/schemas/instances_models_Week"
              type: array
              uniqueItems: true
            - type: "null"
          title: Byweeks
          description: Weeks of the recurrence.
          default: null
        byMonths:
          anyOf:
            - items:
                $ref: "#/components/schemas/instances_models_Month"
              type: array
              uniqueItems: true
            - type: "null"
          title: Bymonths
          description: Months of the recurrence.
          default: null
        bySetPositions:
          anyOf:
            - items:
                $ref: "#/components/schemas/instances_models_Yearday"
              type: array
              uniqueItems: true
            - type: "null"
          title: Bysetpositions
          description: Set positions of the recurrence.
          default: null
        weekStart:
          anyOf:
            - $ref: "#/components/schemas/instances_models_Weekday"
            - type: "null"
          description: Start day of the week.
          default: null
      type: object
      required:
        - frequency
      title: Recurrence
      description: Recurrence rule data.
    instances_models_Second:
      type: integer
      maximum: 59
      minimum: 0
    instances_models_Show:
      properties:
        id:
          type: string
          format: uuid
          title: Id
          description: Identifier of the show.
        title:
          type: string
          title: Title
          description: Title of the show.
        description:
          anyOf:
            - type: string
            - type: "null"
          title: Description
          description: Description of the show.
        events:
          anyOf:
            - items:
                $ref: "#/components/schemas/instances_models_Event"
              type: array
            - type: "null"
          title: Events
          description: Events the show belongs to.
      type: object
      required:
        - id
        - title
        - description
        - events
      title: Show
      description: Show data.
    instances_models_ShowArgsFromEvent:
      properties:
        include:
          $ref: "#/components/schemas/instances_models_ShowIncludeFromShow"
      type: object
      title: ShowArgsFromEvent
      description: Arguments for Event
    instances_models_ShowIncludeFromShow:
      properties:
        events:
          anyOf:
            - type: boolean
            - $ref: "#/components/schemas/instances_models_FindManyEventArgsFromShow"
          title: Events
      type: object
      title: ShowIncludeFromShow
      description: Relational arguments for Show
    instances_models_ShowRelationFilter:
      properties:
        is:
          $ref: "#/components/schemas/instances_models_ShowWhereInput"
        isNot:
          $ref: "#/components/schemas/instances_models_ShowWhereInput"
      type: object
      title: ShowRelationFilter
    instances_models_ShowWhereInput:
      properties:
        id:
          anyOf:
            - type: string
            - $ref: "#/components/schemas/instances_models_StringFilter"
          title: Id
        title:
          anyOf:
            - type: string
            - $ref: "#/components/schemas/instances_models_StringFilter"
          title: Title
        description:
          anyOf:
            - type: string
            - $ref: "#/components/schemas/instances_models_StringFilter"
            - type: "null"
          title: Description
        events:
          $ref: "#/components/schemas/instances_models_EventListRelationFilter"
        and:
          items:
            $ref: "#/components/schemas/instances_models_ShowWhereInput"
          type: array
          title: And
        or:
          items:
            $ref: "#/components/schemas/instances_models_ShowWhereInput"
          type: array
          title: Or
        not:
          items:
            $ref: "#/components/schemas/instances_models_ShowWhereInput"
          type: array
          title: Not
      type: object
      title: ShowWhereInput
      description: Show arguments for searching
    instances_models_StringFilter:
      properties:
        equals:
          type: string
          title: Equals
        notIn:
          items:
            type: string
          type: array
          title: Notin
        lt:
          type: string
          title: Lt
        lte:
          type: string
          title: Lte
        gt:
          type: string
          title: Gt
        gte:
          type: string
          title: Gte
        contains:
          type: string
          title: Contains
        startswith:
          type: string
          title: Startswith
        endswith:
          type: string
          title: Endswith
        in:
          items:
            type: string
          type: array
          title: In
        not:
          anyOf:
            - type: string
            - $ref: "#/components/schemas/instances_models_StringFilter"
          title: Not
        mode:
          type: string
          enum:
            - default
            - insensitive
          title: Mode
        search:
          type: string
          title: Search
      type: object
      title: StringFilter
    instances_models_Termination:
      oneOf:
        - $ref: "#/components/schemas/instances_models_CountTermination"
        - $ref: "#/components/schemas/instances_models_UntilTermination"
      discriminator:
        propertyName: type
        mapping:
          count: "#/components/schemas/beaver_api_routes_instances_models_CountTermination"
          until: "#/components/schemas/beaver_api_routes_instances_models_UntilTermination"
    instances_models_Timedelta:
      type: string
      format: duration
      description: Duration of time.
      examples:
        - PT1H
    instances_models_Timezone:
      type: string
      description: Timezone name.
      examples:
        - Europe/Warsaw
    instances_models_UTCDatetime:
      type: string
      format: date-time
      description: Datetime in UTC.
      examples:
        - 2000-01-01T00:00:00Z
    instances_models_UntilTermination:
      properties:
        type:
          type: string
          const: until
          title: Type
          description: Type of the termination.
          default: until
        until:
          $ref: "#/components/schemas/instances_models_NaiveDatetime"
      type: object
      required:
        - until
      title: UntilTermination
      description: Until termination data.
    instances_models_UpdateRequestData:
      properties:
        start:
          $ref: "#/components/schemas/instances_models_NaiveDatetime"
      type: object
      title: InstanceUpdateInput
      description: Data to update an instance.
    instances_models_UpdateRequestInclude:
      anyOf:
        - $ref: "#/components/schemas/InstanceInclude"
        - type: "null"
    instances_models_Week:
      anyOf:
        - type: integer
          maximum: -1
          minimum: -53
        - type: integer
          maximum: 53
          minimum: 1
    instances_models_Weekday:
      type: string
      enum:
        - monday
        - tuesday
        - wednesday
        - thursday
        - friday
        - saturday
        - sunday
      title: Weekday
      description: Weekday options.
    instances_models_WeekdayRule:
      properties:
        day:
          $ref: "#/components/schemas/instances_models_Weekday"
        occurrence:
          anyOf:
            - $ref: "#/components/schemas/instances_models_Week"
            - type: "null"
          description: Occurrence of the day in the year.
          default: null
      type: object
      required:
        - day
      title: WeekdayRule
      description: Day rule data.
    instances_models_Yearday:
      anyOf:
        - type: integer
          maximum: -1
          minimum: -366
        - type: integer
          maximum: 366
          minimum: 1
    instances_models__EventWhereUnique_id_Input:
      properties:
        id:
          type: string
          title: Id
      type: object
      required:
        - id
      title: _EventWhereUnique_id_Input
    instances_models__Event_RelevanceInner:
      properties:
        fields:
          items:
            type: string
            enum:
              - id
              - type
              - showId
          type: array
          title: Fields
        search:
          type: string
          title: Search
        sort:
          type: string
          enum:
            - asc
            - desc
          title: Sort
      type: object
      required:
        - fields
        - search
        - sort
      title: _Event_RelevanceInner
    instances_models__Event_RelevanceOrderByInput:
      properties:
        _relevance:
          $ref: "#/components/schemas/instances_models__Event_RelevanceInner"
      type: object
      required:
        - _relevance
      title: _Event_RelevanceOrderByInput
    instances_models__Event_id_OrderByInput:
      properties:
        id:
          type: string
          enum:
            - asc
            - desc
          title: Id
      type: object
      required:
        - id
      title: _Event_id_OrderByInput
    instances_models__Event_showId_OrderByInput:
      properties:
        showId:
          type: string
          enum:
            - asc
            - desc
          title: Showid
      type: object
      required:
        - showId
      title: _Event_showId_OrderByInput
    instances_models__Event_type_OrderByInput:
      properties:
        type:
          type: string
          enum:
            - asc
            - desc
          title: Type
      type: object
      required:
        - type
      title: _Event_type_OrderByInput
    shows_models_CountTermination:
      properties:
        type:
          type: string
          const: count
          title: Type
          description: Type of the termination.
          default: count
        count:
          type: integer
          exclusiveMinimum: 0
          title: Count
          description: Number of instances of recurring event.
      type: object
      required:
        - count
      title: CountTermination
      description: Count termination data.
    shows_models_CreateRequestData:
      properties:
        id:
          type: string
          title: Id
        description:
          anyOf:
            - type: string
            - type: "null"
          title: Description
        title:
          type: string
          title: Title
      type: object
      required:
        - title
      title: ShowCreateWithoutRelationsInput
      description: >-
        Required arguments to the Show create method, without relations
    shows_models_CreateRequestInclude:
      anyOf:
        - $ref: "#/components/schemas/ShowInclude"
        - type: "null"
    shows_models_DeleteRequestId:
      type: string
      format: uuid
    shows_models_Event:
      properties:
        id:
          type: string
          format: uuid
          title: Id
          description: Identifier of the event.
        type:
          $ref: "#/components/schemas/shows_models_EventType"
        showId:
          anyOf:
            - type: string
              format: uuid
            - type: "null"
          title: Showid
          description: Identifier of the show the event belongs to.
        show:
          anyOf:
            - $ref: "#/components/schemas/shows_models_Show"
            - type: "null"
          description: Show the event belongs to.
        start:
          $ref: "#/components/schemas/shows_models_NaiveDatetime"
        duration:
          $ref: "#/components/schemas/shows_models_Timedelta"
        timezone:
          $ref: "#/components/schemas/shows_models_Timezone"
        recurrence:
          anyOf:
            - $ref: "#/components/schemas/shows_models_Recurrence"
            - type: "null"
          description: Recurrence rule of the event.
          default: null
        include:
          anyOf:
            - items:
                $ref: "#/components/schemas/shows_models_Inclusion"
              type: array
              uniqueItems: true
            - type: "null"
          title: Include
          description: Included instances of the event.
          default: null
        exclude:
          anyOf:
            - items:
                $ref: "#/components/schemas/shows_models_Exclusion"
              type: array
              uniqueItems: true
            - type: "null"
          title: Exclude
          description: Excluded instances of the event.
          default: null
      type: object
      required:
        - id
        - type
        - showId
        - show
        - start
        - duration
        - timezone
      title: Event
      description: Event data.
    shows_models_EventIncludeFromEvent:
      properties:
        show:
          anyOf:
            - type: boolean
            - $ref: "#/components/schemas/shows_models_ShowArgsFromEvent"
          title: Show
      type: object
      title: EventIncludeFromEvent
      description: Relational arguments for Event
    shows_models_EventListRelationFilter:
      properties:
        some:
          $ref: "#/components/schemas/shows_models_EventWhereInput"
        none:
          $ref: "#/components/schemas/shows_models_EventWhereInput"
        every:
          $ref: "#/components/schemas/shows_models_EventWhereInput"
      type: object
      title: EventListRelationFilter
    shows_models_EventType:
      type: string
      enum:
        - live
        - replay
        - prerecorded
      title: EventType
    shows_models_EventWhereInput:
      properties:
        id:
          anyOf:
            - type: string
            - $ref: "#/components/schemas/shows_models_StringFilter"
          title: Id
        type:
          $ref: "#/components/schemas/shows_models_EventType"
        showId:
          anyOf:
            - type: string
            - $ref: "#/components/schemas/shows_models_StringFilter"
            - type: "null"
          title: Showid
        show:
          $ref: "#/components/schemas/shows_models_ShowRelationFilter"
        and:
          items:
            $ref: "#/components/schemas/shows_models_EventWhereInput"
          type: array
          title: And
        or:
          items:
            $ref: "#/components/schemas/shows_models_EventWhereInput"
          type: array
          title: Or
        not:
          items:
            $ref: "#/components/schemas/shows_models_EventWhereInput"
          type: array
          title: Not
      type: object
      title: EventWhereInput
      description: Event arguments for searching
    shows_models_Exclusion:
      properties:
        start:
          $ref: "#/components/schemas/shows_models_NaiveDatetime"
      type: object
      required:
        - start
      title: Exclusion
      description: Exclusion data.
    shows_models_FindManyEventArgsFromShow:
      properties:
        take:
          type: integer
          title: Take
        skip:
          type: integer
          title: Skip
        orderBy:
          anyOf:
            - $ref: "#/components/schemas/shows_models__Event_id_OrderByInput"
            - $ref: "#/components/schemas/shows_models__Event_type_OrderByInput"
            - $ref: "#/components/schemas/shows_models__Event_showId_OrderByInput"
            - $ref: "#/components/schemas/shows_models__Event_RelevanceOrderByInput"
            - items:
                anyOf:
                  - $ref: "#/components/schemas/shows_models__Event_id_OrderByInput"
                  - $ref: "#/components/schemas/shows_models__Event_type_OrderByInput"
                  - $ref: "#/components/schemas/shows_models__Event_showId_OrderByInput"
                  - $ref: "#/components/schemas/shows_models__Event_RelevanceOrderByInput"
              type: array
          title: Orderby
        where:
          $ref: "#/components/schemas/shows_models_EventWhereInput"
        cursor:
          $ref: "#/components/schemas/shows_models__EventWhereUnique_id_Input"
        distinct:
          items:
            type: string
            enum:
              - id
              - type
              - showId
          type: array
          title: Distinct
        include:
          $ref: "#/components/schemas/shows_models_EventIncludeFromEvent"
      type: object
      title: FindManyEventArgsFromShow
      description: Arguments for Show
    shows_models_Frequency:
      type: string
      enum:
        - secondly
        - minutely
        - hourly
        - daily
        - weekly
        - monthly
        - yearly
      title: Frequency
      description: Frequency options.
    shows_models_GetRequestId:
      type: string
      format: uuid
    shows_models_GetRequestInclude:
      anyOf:
        - $ref: "#/components/schemas/ShowInclude"
        - type: "null"
    shows_models_Hour:
      type: integer
      maximum: 23
      minimum: 0
    shows_models_Inclusion:
      properties:
        start:
          $ref: "#/components/schemas/shows_models_NaiveDatetime"
      type: object
      required:
        - start
      title: Inclusion
      description: Inclusion data.
    shows_models_ListRequestInclude:
      anyOf:
        - $ref: "#/components/schemas/ShowInclude"
        - type: "null"
    shows_models_ListRequestLimit:
      anyOf:
        - type: integer
        - type: "null"
    shows_models_ListRequestOffset:
      anyOf:
        - type: integer
        - type: "null"
    shows_models_ListRequestOrder:
      anyOf:
        - $ref: "#/components/schemas/ShowOrderByInput"
        - items:
            $ref: "#/components/schemas/ShowOrderByInput"
          type: array
        - type: "null"
    shows_models_ListRequestWhere:
      anyOf:
        - $ref: "#/components/schemas/shows_models_ShowWhereInput"
        - type: "null"
    shows_models_ListResponseResults:
      properties:
        count:
          type: integer
          title: Count
          description: Total number of shows that matched the query.
        limit:
          anyOf:
            - type: integer
            - type: "null"
          title: Limit
          description: Maximum number of returned shows.
        offset:
          anyOf:
            - type: integer
            - type: "null"
          title: Offset
          description: Number of shows skipped.
        shows:
          items:
            $ref: "#/components/schemas/shows_models_Show"
          type: array
          title: Shows
          description: Shows that matched the request.
      type: object
      required:
        - count
        - limit
        - offset
        - shows
      title: ShowList
      description: List of shows.
    shows_models_Minute:
      type: integer
      maximum: 59
      minimum: 0
    shows_models_Month:
      type: integer
      maximum: 12
      minimum: 1
    shows_models_Monthday:
      anyOf:
        - type: integer
          maximum: -1
          minimum: -31
        - type: integer
          maximum: 31
          minimum: 1
    shows_models_NaiveDatetime:
      type: string
      format: date-time
      description: Datetime without timezone.
      examples:
        - 2000-01-01T00:00:00
    shows_models_Recurrence:
      properties:
        frequency:
          $ref: "#/components/schemas/shows_models_Frequency"
        termination:
          anyOf:
            - $ref: "#/components/schemas/shows_models_Termination"
            - type: "null"
          description: Termination of the recurrence.
          default: null
        interval:
          anyOf:
            - type: integer
              exclusiveMinimum: 0
            - type: "null"
          title: Interval
          description: Interval of the recurrence.
          default: null
        bySeconds:
          anyOf:
            - items:
                $ref: "#/components/schemas/shows_models_Second"
              type: array
              uniqueItems: true
            - type: "null"
          title: Byseconds
          description: Seconds of the recurrence.
          default: null
        byMinutes:
          anyOf:
            - items:
                $ref: "#/components/schemas/shows_models_Minute"
              type: array
              uniqueItems: true
            - type: "null"
          title: Byminutes
          description: Minutes of the recurrence.
          default: null
        byHours:
          anyOf:
            - items:
                $ref: "#/components/schemas/shows_models_Hour"
              type: array
              uniqueItems: true
            - type: "null"
          title: Byhours
          description: Hours of the recurrence.
          default: null
        byWeekdays:
          anyOf:
            - items:
                $ref: "#/components/schemas/shows_models_WeekdayRule"
              type: array
              uniqueItems: true
            - type: "null"
          title: Byweekdays
          description: Weekdays of the recurrence.
          default: null
        byMonthdays:
          anyOf:
            - items:
                $ref: "#/components/schemas/shows_models_Monthday"
              type: array
              uniqueItems: true
            - type: "null"
          title: Bymonthdays
          description: Monthdays of the recurrence.
          default: null
        byYeardays:
          anyOf:
            - items:
                $ref: "#/components/schemas/shows_models_Yearday"
              type: array
              uniqueItems: true
            - type: "null"
          title: Byyeardays
          description: Yeardays of the recurrence.
          default: null
        byWeeks:
          anyOf:
            - items:
                $ref: "#/components/schemas/shows_models_Week"
              type: array
              uniqueItems: true
            - type: "null"
          title: Byweeks
          description: Weeks of the recurrence.
          default: null
        byMonths:
          anyOf:
            - items:
                $ref: "#/components/schemas/shows_models_Month"
              type: array
              uniqueItems: true
            - type: "null"
          title: Bymonths
          description: Months of the recurrence.
          default: null
        bySetPositions:
          anyOf:
            - items:
                $ref: "#/components/schemas/shows_models_Yearday"
              type: array
              uniqueItems: true
            - type: "null"
          title: Bysetpositions
          description: Set positions of the recurrence.
          default: null
        weekStart:
          anyOf:
            - $ref: "#/components/schemas/shows_models_Weekday"
            - type: "null"
          description: Start day of the week.
          default: null
      type: object
      required:
        - frequency
      title: Recurrence
      description: Recurrence rule data.
    shows_models_Second:
      type: integer
      maximum: 59
      minimum: 0
    shows_models_Show:
      properties:
        id:
          type: string
          format: uuid
          title: Id
          description: Identifier of the show.
        title:
          type: string
          title: Title
          description: Title of the show.
        description:
          anyOf:
            - type: string
            - type: "null"
          title: Description
          description: Description of the show.
        events:
          anyOf:
            - items:
                $ref: "#/components/schemas/shows_models_Event"
              type: array
            - type: "null"
          title: Events
          description: Events the show belongs to.
      type: object
      required:
        - id
        - title
        - description
        - events
      title: Show
      description: Show data.
    shows_models_ShowArgsFromEvent:
      properties:
        include:
          $ref: "#/components/schemas/shows_models_ShowIncludeFromShow"
      type: object
      title: ShowArgsFromEvent
      description: Arguments for Event
    shows_models_ShowIncludeFromShow:
      properties:
        events:
          anyOf:
            - type: boolean
            - $ref: "#/components/schemas/shows_models_FindManyEventArgsFromShow"
          title: Events
      type: object
      title: ShowIncludeFromShow
      description: Relational arguments for Show
    shows_models_ShowRelationFilter:
      properties:
        is:
          $ref: "#/components/schemas/shows_models_ShowWhereInput"
        isNot:
          $ref: "#/components/schemas/shows_models_ShowWhereInput"
      type: object
      title: ShowRelationFilter
    shows_models_ShowWhereInput:
      properties:
        id:
          anyOf:
            - type: string
            - $ref: "#/components/schemas/shows_models_StringFilter"
          title: Id
        title:
          anyOf:
            - type: string
            - $ref: "#/components/schemas/shows_models_StringFilter"
          title: Title
        description:
          anyOf:
            - type: string
            - $ref: "#/components/schemas/shows_models_StringFilter"
            - type: "null"
          title: Description
        events:
          $ref: "#/components/schemas/shows_models_EventListRelationFilter"
        and:
          items:
            $ref: "#/components/schemas/shows_models_ShowWhereInput"
          type: array
          title: And
        or:
          items:
            $ref: "#/components/schemas/shows_models_ShowWhereInput"
          type: array
          title: Or
        not:
          items:
            $ref: "#/components/schemas/shows_models_ShowWhereInput"
          type: array
          title: Not
      type: object
      title: ShowWhereInput
      description: Show arguments for searching
    shows_models_StringFilter:
      properties:
        equals:
          type: string
          title: Equals
        notIn:
          items:
            type: string
          type: array
          title: Notin
        lt:
          type: string
          title: Lt
        lte:
          type: string
          title: Lte
        gt:
          type: string
          title: Gt
        gte:
          type: string
          title: Gte
        contains:
          type: string
          title: Contains
        startswith:
          type: string
          title: Startswith
        endswith:
          type: string
          title: Endswith
        in:
          items:
            type: string
          type: array
          title: In
        not:
          anyOf:
            - type: string
            - $ref: "#/components/schemas/shows_models_StringFilter"
          title: Not
        mode:
          type: string
          enum:
            - default
            - insensitive
          title: Mode
        search:
          type: string
          title: Search
      type: object
      title: StringFilter
    shows_models_Termination:
      oneOf:
        - $ref: "#/components/schemas/shows_models_CountTermination"
        - $ref: "#/components/schemas/shows_models_UntilTermination"
      discriminator:
        propertyName: type
        mapping:
          count: "#/components/schemas/beaver_api_routes_shows_models_CountTermination"
          until: "#/components/schemas/beaver_api_routes_shows_models_UntilTermination"
    shows_models_Timedelta:
      type: string
      format: duration
      description: Duration of time.
      examples:
        - PT1H
    shows_models_Timezone:
      type: string
      description: Timezone name.
      examples:
        - Europe/Warsaw
    shows_models_UntilTermination:
      properties:
        type:
          type: string
          const: until
          title: Type
          description: Type of the termination.
          default: until
        until:
          $ref: "#/components/schemas/shows_models_NaiveDatetime"
      type: object
      required:
        - until
      title: UntilTermination
      description: Until termination data.
    shows_models_UpdateRequestData:
      properties:
        id:
          type: string
          title: Id
        title:
          type: string
          title: Title
        description:
          anyOf:
            - type: string
            - type: "null"
          title: Description
      type: object
      title: ShowUpdateManyMutationInput
      description: Arguments for updating many records
    shows_models_UpdateRequestId:
      type: string
      format: uuid
    shows_models_UpdateRequestInclude:
      anyOf:
        - $ref: "#/components/schemas/ShowInclude"
        - type: "null"
    shows_models_Week:
      anyOf:
        - type: integer
          maximum: -1
          minimum: -53
        - type: integer
          maximum: 53
          minimum: 1
    shows_models_Weekday:
      type: string
      enum:
        - monday
        - tuesday
        - wednesday
        - thursday
        - friday
        - saturday
        - sunday
      title: Weekday
      description: Weekday options.
    shows_models_WeekdayRule:
      properties:
        day:
          $ref: "#/components/schemas/shows_models_Weekday"
        occurrence:
          anyOf:
            - $ref: "#/components/schemas/shows_models_Week"
            - type: "null"
          description: Occurrence of the day in the year.
          default: null
      type: object
      required:
        - day
      title: WeekdayRule
      description: Day rule data.
    shows_models_Yearday:
      anyOf:
        - type: integer
          maximum: -1
          minimum: -366
        - type: integer
          maximum: 366
          minimum: 1
    shows_models__EventWhereUnique_id_Input:
      properties:
        id:
          type: string
          title: Id
      type: object
      required:
        - id
      title: _EventWhereUnique_id_Input
    shows_models__Event_RelevanceInner:
      properties:
        fields:
          items:
            type: string
            enum:
              - id
              - type
              - showId
          type: array
          title: Fields
        search:
          type: string
          title: Search
        sort:
          type: string
          enum:
            - asc
            - desc
          title: Sort
      type: object
      required:
        - fields
        - search
        - sort
      title: _Event_RelevanceInner
    shows_models__Event_RelevanceOrderByInput:
      properties:
        _relevance:
          $ref: "#/components/schemas/shows_models__Event_RelevanceInner"
      type: object
      required:
        - _relevance
      title: _Event_RelevanceOrderByInput
    shows_models__Event_id_OrderByInput:
      properties:
        id:
          type: string
          enum:
            - asc
            - desc
          title: Id
      type: object
      required:
        - id
      title: _Event_id_OrderByInput
    shows_models__Event_showId_OrderByInput:
      properties:
        showId:
          type: string
          enum:
            - asc
            - desc
          title: Showid
      type: object
      required:
        - showId
      title: _Event_showId_OrderByInput
    shows_models__Event_type_OrderByInput:
      properties:
        type:
          type: string
          enum:
            - asc
            - desc
          title: Type
      type: object
      required:
        - type
      title: _Event_type_OrderByInput
    sse_models_EventType:
      type: string
      enum:
        - test
      title: EventType
      description: Event types.
